namespace CJR.PDF.Read.Domain
{
    public enum TokenType
    {
        Comment, BooleanObject, IntObject, RealObject, StringObject, NameObject, DictionaryObject, ArrayObject, 
        StreamObject, NullObject, 
    }
}
